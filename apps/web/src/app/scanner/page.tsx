import { ComingSoon } from "@/components/ui/ComingSoon";

export default function ScannerPage() {
  return (
    <ComingSoon
      title="Market Scanner"
      description="Scan the market for RSI extremes, EMA crossovers, VWAP breaks, volume spikes, OI changes and more."
      phase="Phase 8"
    />
  );
}
