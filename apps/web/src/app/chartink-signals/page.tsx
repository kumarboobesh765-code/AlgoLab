import { ComingSoon } from "@/components/marketing/ComingSoon";

export default function ChartinkSignalsPage() {
  return (
    <ComingSoon
      title="Chartink Signals"
      description="Point Chartink scan alerts at your StrategyLab webhook URL — the screener takes the trade. Backtest the screener first, then go live."
      expectedIn="Milestone D"
    />
  );
}
