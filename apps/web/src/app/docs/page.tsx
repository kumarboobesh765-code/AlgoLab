import { ComingSoon } from "@/components/marketing/ComingSoon";

export default function DocsPage() {
  return (
    <ComingSoon
      title="Documentation"
      description="API reference, builder guides, broker setup walkthroughs, and the public roadmap. Until the docs site launches, the source of truth is the /roadmap page and the README in the GitHub repo."
      expectedIn="Milestone E"
    />
  );
}
